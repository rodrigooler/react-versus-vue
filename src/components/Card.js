import styled from "styled-components";

const Card = styled.div`
  width: 350px;
  height: 500px;
  overflow: hidden;
  border-radius: 10px;
  box-shadow: 3px 3px 20px rgba(0, 0, 0, 0.5);
  text-align: center;
  background: #FFFFFF;
`;

export default Card;
